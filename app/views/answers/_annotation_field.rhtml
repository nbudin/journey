<% if logged_in? and logged_in_person.permitted?("edit", @question.questionnaire) -%>
  <% if @editing -%>
    <%= text_area_tag "answer[#{@question.id}]", value, :rows => 4 %>
  <% else -%>
    <%= simple_format value %>
  <% end -%>
<% end -%>
