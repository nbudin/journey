<% value = (value.present? && value.to_s != 'false') %>
<% if @editing -%>
	<%= check_box_tag "answer[#{@question.id}]", 'checked', value %>
<% else -%>
	<%= check_box_tag "answer[#{@question.id}]", 'checked', value, :disabled => true %>
<% end -%>
