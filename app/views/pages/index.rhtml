<h1>Listing pages</h1>

<table>
  <tr>
  </tr>

<% for page in @pages %>
  <tr>
    <td><%= link_to 'Show', questionnaire_page_path(@questionnaire, page) %></td>
    <td><%= link_to 'Edit', edit_questionnaire_page_path(@questionnaire, page) %></td>
    <td><%= link_to 'Destroy', questionnaire_page_path(@questionnaire, page), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New page', new_questionnaire_page_path(@questionnaire) %>
