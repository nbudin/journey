<%= render :partial => "questionnaires/toplevel_tabs" %>

<ul class="tabstrip2">
	<%= tabstrip_li "Publish", [@questionnaire, :publish], :level => 2, :action => 'index' %>
	<%= tabstrip_li "Settings", [:settings, @questionnaire, :publish], :level => 2, :action => 'settings' %>
</ul>