<% obj_id = "#{obj.class.name.tableize.singularize}_#{field}" -%>
<%= text_area_tag("#{obj.class.name.tableize.singularize}[#{field}]", obj.send(field), :rows => 4, :class => "tinymce",
                  :style => "width: 60em;", :id => obj_id) %>
<%= tinymce %>