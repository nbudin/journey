<% form_for(@response, :url => response_path(@questionnaire, @response)) do %>
  <%= render :partial => 'response', :locals => { :resp => @response } %>
  
  <%= submit_tag "Save changes" %>
<% end %>