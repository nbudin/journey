div.topbar {
    position: absolute;
    overflow: hidden;
}

form.filterbar {
    margin-top: 0;
}

form.filterbar input[type=submit] {
    width: 100px;
}

div#toolbox {
    position: absolute;
    overflow: hidden;
}
