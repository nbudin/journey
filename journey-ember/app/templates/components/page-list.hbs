{{#sortable-group tagName="ul" classNames="pagelist" handle=".draghandle" as |group|}}
  {{#each pages as |page|}}
    {{#sortable-item tagName="li" handle=".draghandle"}}
      {{#link-to "admin.questionnaire.pages.page" page tagName="div" href=false}}
        <a {{action "setCurrentPage" page}}>
          <div class="draghandle"></div>
          <div class="caption">
            {{page.title}}
          </div>
          <button class="delete-page" {{action "deletePage" page bubbles=false}}></button>
        </a>
      {{/link-to}}
    {{/sortable-item}}
  {{/each}}
{{/sortable-group}}
